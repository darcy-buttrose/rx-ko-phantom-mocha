/// <references path="../../../typings/browser.d.ts" />
import {expect} from "chai";

describe('Given a user is on the home page', () => {
    it('should work',() => {
        expect(true).to.equal(true);
    })
})